import { CapitilizePipe } from './capitilize.pipe';

describe('CapitilizePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitilizePipe();
    expect(pipe).toBeTruthy();
  });
});
